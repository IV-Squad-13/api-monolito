server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: api-monolito
  config:
    import: file:.env.properties

  datasource:
    url: jdbc:postgresql://ep-autumn-dawn-ac53an9n-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_DO3zkrdv2oVT
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

springdoc:
  api-docs:
    path: /api-docs